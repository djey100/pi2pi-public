# pi2pi frontend — build-time network selection (non-secret).
# Loaded automatically by Vitest (test mode) so the existing test suite keeps
# running against Arc Testnet with zero extra config. See .env.production and
# arc/src/helpers.js for details.
VITE_NETWORK=arc-testnet
