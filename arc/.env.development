# pi2pi frontend — build-time network selection (non-secret).
# Loaded automatically by `vite`/`vite dev` (development mode) so the local
# dev server keeps working with zero extra config. See .env.production and
# arc/src/helpers.js for details.
VITE_NETWORK=arc-testnet
