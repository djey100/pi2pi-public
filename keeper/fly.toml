app = 'pi2pi-keeper'
primary_region = 'syd'

[build]

[env]
  NETWORK = "arc-testnet"
  LOOP_INTERVAL = "15"
  BATCH_SIZE = "100"
  DRY_RUN = "false"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
